name: Build

on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v3

            - name: Setup NodeJS
              uses: actions/setup-node@v6

            - name: Install pnpm
              uses: pnpm/action-setup@v3
              with:
                  version: 10

            - run: pnpm install
            - run: pnpm build
